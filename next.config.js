/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: { unoptimized: true },
    // If your site is at username.github.io/repo-name, set basePath: '/repo-name'
    // basePath: '/zabzablab',
}

module.exports = nextConfig
