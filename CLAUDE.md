# ZabZabLab

Artist-made wallpaper shop. Next.js 15 App Router, TypeScript, CSS Modules.
Owner: Vera. Dev server: `npm run dev` (usually port 3002 if 3000 is occupied).

## Skill routing

When editing any marketing page (`app/page.tsx`, `app/about/page.tsx`, `app/designs/**`, `app/for-interior-designers/**`), invoke the `/ecommerce` skill first to apply course principles before making changes.
